#pragma once
#include "EquipmentType.h"

void sortById(Equipment* list, int size);

void sortByName(Equipment* list, int size);

void sortByType(Equipment* list, int size);

void sortByQuantity(Equipment* list, int size);

void sortByDate(Equipment* list, int size);